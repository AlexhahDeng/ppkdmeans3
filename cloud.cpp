#include "cloud.h"

cloud::cloud(int data_num, int dimension, Comparator* comparator){
    this->data_num = data_num;
    this->dimension = dimension;
    this->comparator = comparator;
}

vector<vector<int>> cloud::calculate_e_f(vector<int>N){

}